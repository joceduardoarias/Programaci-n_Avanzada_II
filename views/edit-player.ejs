<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Modificar Jugador</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <!-- Toolbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <button class="btn btn-outline-secondary" onclick="history.back()">Atrás</button>
      <form class="d-flex ms-auto" action="/logout" method="POST">
        <button class="btn btn-outline-danger" type="submit">Salir</button>
      </form>
    </div>
  </nav>

  <div class="container mt-5">
    <h1 class="text-center mb-4">Modificar Jugador</h1>
    <form action="/footballplayers/update/<%= player.id %>?_method=PUT" method="POST" class="needs-validation" novalidate>        
      <div class="row mb-3">
        <div class="col-md-6">
          <label for="nombre" class="form-label">Nombre</label>
          <input type="text" class="form-control" id="nombre" name="nombre" value="<%= player.nombre %>" required>
          <div class="invalid-feedback">
            Por favor, ingresa el nombre del jugador.
          </div>
        </div>
        <div class="col-md-6">
          <label for="edad" class="form-label">Edad</label>
          <input type="number" class="form-control" id="edad" name="edad" value="<%= player.edad %>" required>
          <div class="invalid-feedback">
            Por favor, ingresa la edad del jugador.
          </div>
        </div>
      </div>

      <div class="mb-3">
        <label for="equipo" class="form-label">Equipo</label>
        <input type="text" class="form-control" id="equipo" name="equipo" value="<%= player.equipo %>" required>
        <div class="invalid-feedback">
          Por favor, ingresa el equipo del jugador.
        </div>
      </div>

      <!-- Add more fields as necessary -->

      <div class="text-center">
        <button type="submit" class="btn btn-primary">Guardar Cambios</button>
      </div>
    </form>
  </div>

  <script>
    // Bootstrap 5 form validation
    (function () {
      'use strict'
      var forms = document.querySelectorAll('.needs-validation')
      Array.prototype.slice.call(forms).forEach(function (form) {
        form.addEventListener('submit', function (event) {
          if (!form.checkValidity()) {
            event.preventDefault()
            event.stopPropagation()
          }
          form.classList.add('was-validated')
        }, false)
      })
    })();
  </script>
</body>
</html>